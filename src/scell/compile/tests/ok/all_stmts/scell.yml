main:
  from_image: from
  shell: shell
  hang: hang
  workspace: /app
  copy:
    - copy_file.txt .
  build:
    - apt-get update
    - apt-get install -y git
  env:
    - MY_VAR=hello
    - PATH=/usr/local/bin:/usr/bin