# MVP features

- [ ] Recursively parse `Cells` based on the `SCell` files, forming a linked list which would be executed in the reverse order than it was added.
- [ ] Use BuildKit cache feature, to cache each Cell individually.
- [ ] Only docker as a backend
- [ ] 