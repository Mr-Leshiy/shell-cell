
main:
  from: /Users/global.yml+something
  workspace: workdir
  shell: [/bin/bash, -c]
  hang: while true; do sleep 3600; done
  copy:
    - ../../Work/catalyst-libs/rust/Cargo.toml .
  build:
    - echo "hello world"
