main:
  from: python:3.14-trixie
  workspace: /app
  shell: /bin/bash
  hang: while true; do sleep 3600; done
  copy:
    - data/copy_test.txt copy_test.txt
  env:
    - ENV_TEST="env works!"
  build:
    - echo 'build works!' > build_test.txt
  config:
    mounts:
      - ./mount-source:/app/mnt
    ports:
      - "4321:4321"
